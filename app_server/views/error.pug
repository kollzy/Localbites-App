extends layout

block content
  h1= message || 'An error occurred'
  if error && error.status
    h2= `Status: ${error.status}`
  else
    h2= 'Status: Unknown'
  if error && error.stack
    pre #{error.stack}
  else
    p No additional error details available.
